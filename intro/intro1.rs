

/*use std::mem::size_of;
#[derive(Debug)]
enum Canvas {
    Point {x:i32, y:i32},
    Description(String),
}

fn inspect(canvas : Canvas)
{
    match canvas {
        Canvas::Point{x,y} => println!("Point is {}, y:{}", x, y),
        Canvas::Description(s) => println!("Description is {}", s)        
    }
}

fn main() {
    let pt = Canvas::Point {x:10, y:10};
    let descr = Canvas::Description("2D canvas".to_string());

    inspect(pt);
    inspect(descr);


    println!("------------------------");
    println!("Size of Canvas enum is: {}", size_of::<Canvas>());
    println!("  --> Size of String : {}",  size_of::<String>());

}*/


fn main()
{
   
}